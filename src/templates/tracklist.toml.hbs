medium-ids = [
  "{{artist_id}}/{{parameterize (default-name album.names)}}/default/digital1",
]
{{#each album.songs}}
[[tracks]]
position = {{position}}
sond-id = "{{../artist_id}}/{{parameterize (default-name names)}}"
duration = "{{format-duration duration}}"
{{/each}}
